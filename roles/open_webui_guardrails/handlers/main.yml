---
- name: open_webui_restart_stack
  ansible.builtin.command:
    argv:
      - docker
      - compose
      - -f
      - "{{ open_webui_compose_file }}"
      - up
      - -d
      - --force-recreate
