---
# ============================================================
# handlers/main.yml
# Handler'ы роли nvidia_container_toolkit.
# ============================================================

- name: Restart docker after NVIDIA runtime configuration
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true
  listen: nvidia_container_toolkit_restart_docker